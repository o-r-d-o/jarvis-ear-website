# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — STT (falls back to manual input if not set)
DEEPGRAM_API_KEY=...

# Optional — TTS (falls back to macOS 'say' command if not set)
OPENAI_API_KEY=sk-...

# Optional — Actions (dry run if not set)
SLACK_BOT_TOKEN=xoxb-...
GITHUB_TOKEN=ghp_...
